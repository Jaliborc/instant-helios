doctype html
head
  title= site.basics.name
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1, user-scalable=no')
  link(rel='stylesheet' href='main.css')
  noscript
    link(rel='stylesheet' href='noscript.css')
      
body(itemtype="http://schema.org/Person")
  #page-wrapper
    include mixins.pug
    include header.pug
    if page.banner
      include banner.pug
    if page.title
      include content.pug
    include carousel.pug
    include footer.pug
    if site.recaptcha-key
      include recaptcha.pug
  
script(src='scripts.js')
